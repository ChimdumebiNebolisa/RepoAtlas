# Accessible report navigation inputs

- Completed report: bundled `repo-ts` sample through the sample analysis action.
- Invalid ZIP: a temporary `.txt` file containing `hello` inside `.playwright-reports/`.
- Missing report: a fresh random UUID generated by the Playwright test.
- Expired share: a sample report share whose controlled local expiry timestamp is moved one minute into the past.
